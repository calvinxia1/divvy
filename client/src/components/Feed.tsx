import React, {useState} from 'react'
interface Props {
  
}
const Feed = () => {
    const [page, setPage] = useState(-1)
    const feeds = []
    
  return (
    <div></div>
  )
}

export default Feed